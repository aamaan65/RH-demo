# Minimal runtime requirements (direct imports used by backend code)
Flask==2.3.3
flask-cors==6.0.1
python-dotenv==1.0.0

# MongoDB (includes bson)
pymongo==4.5.0
dnspython==2.4.2

# Google ID token verification (used in app.py)
oauth2client==4.1.3

# LangChain + OpenAI
langchain==0.3.27
langchain-community==0.3.31
langchain-core==0.3.80
langchain-openai==0.3.35
openai==2.8.1
tiktoken==0.7.0

# Milvus vector store
pymilvus==2.4.4

# BigQuery (used in token_module.py)
google-cloud-bigquery==3.38.0
google-auth==2.41.1

# GCS transcript listing/reading (used in app.py via fsspec/gcsfs)
fsspec==2025.12.0
gcsfs==2025.12.0
certifi==2025.11.12

# Optional: monitoring_module.py dependencies (app.py will run without these)
# NOTE: monitoring_module.py currently imports `closest` (not in this repo); if you want monitoring enabled,
# you must provide that module AND install the deps below.
whylogs==1.6.4
langkit==0.0.35
whylabs-textstat==0.7.4
jaeger-client==4.8.0
opentracing==2.4.0
sentence-transformers==5.1.2
nltk
closest